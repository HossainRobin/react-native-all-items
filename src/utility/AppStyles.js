import { StyleSheet } from "react-native";

export default appStyles = StyleSheet.create({
  cardBox: {
    fontSize: 30,
    color: "green",
    backgroundColor: "red",
    padding: 15,
    height: 100,
  },
});
